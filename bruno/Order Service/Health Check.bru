meta {
  name: Health Check
  type: http
  seq: 1
}

get {
  url: {{orderServiceUrl}}/health
  body: none
  auth: none
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
