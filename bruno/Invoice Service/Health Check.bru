meta {
  name: Health Check
  type: http
  seq: 1
}

get {
  url: {{invoiceServiceUrl}}
  body: none
  auth: none
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("Response contains hello message", function() {
    expect(res.getBody()).to.be.a('string');
  });
}
